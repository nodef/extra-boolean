/**
 * Check if value is false.
 * @param a a boolean
 * @example
 * ```javascript
 * not(false) === true
 * not(true)  === false
 * ```
 */
function not(a: boolean): boolean {
  return !a;
}
export default not;
